# T-003: DB環境構築・マイグレーション

**ステータス:** 未着手
**フェーズ:** Step 2（基盤）
**依存:** T-002

## 概要
PostgreSQLのローカル開発環境構築（Docker Compose）。SQLAlchemy + Alembicでマイグレーション基盤を構築。

## 完了条件
- [ ] Docker Compose で PostgreSQL 起動
- [ ] SQLAlchemy 接続設定
- [ ] Alembic 初期化・設定
- [ ] 主要テーブル作成マイグレーション
  - users (id, email, hashed_password, created_at)
  - tasks (id, title, description, status, priority, due_date, category_id, completed_at, deleted_at, created_at, updated_at)
  - categories (id, name, color, created_at)
  - labels (id, name, color, created_at)
  - task_labels (task_id, label_id)
- [ ] `alembic upgrade head` 実行確認
